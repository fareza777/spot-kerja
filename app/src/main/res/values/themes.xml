<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Spotkerja" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
        <item name="android:statusBarColor">@android:color/black</item>
        <item name="android:navigationBarColor">@android:color/black</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <!-- Splash: ikon app di tengah layar gelap, lalu transisi ke tema utama. -->
    <style name="Theme.Spotkerja.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">#070D0B</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.Spotkerja</item>
    </style>
</resources>
